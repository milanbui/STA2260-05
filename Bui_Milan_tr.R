data <- c(1, -3, 5, 7, 4, 11, -13, 2); data # after the semi-colon just prints the contents of the variable

sum(data) # Only the function is being run here, everything after the # sign is a comment and ignored when run! Feel free to place comments like this.

mean(data)

data^2

data + 5

?mean # ? before some functions gives the documentation for it